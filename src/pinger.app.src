%% Copyright
{application, pinger, [
    {description, ""},
    {vsn, "1"},
    {registered, []},
    {applications, [
        kernel,
        stdlib,
        inets
    ]},
    {modules, { pinger, [http_pinger, http_pinger_sup, tcp_server, tcp_server_sup, pinger_app, pinger_app_sup]}},
    {mod, { config, "ping.conf"}},
    {env, []}
]}.